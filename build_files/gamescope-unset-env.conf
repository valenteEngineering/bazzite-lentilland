[Service]
# Strip compositor env leaked from a previous Hyprland session so gamescope runs on DRM
# instead of trying to nest into the dead wayland-1 socket (black-screen loop on switch).
UnsetEnvironment=WAYLAND_DISPLAY DISPLAY XDG_SESSION_TYPE XDG_CURRENT_DESKTOP
